= render partial: 'button'
.modal.fade{id: "explain_#{@group}_#{@key}", tabindex: -1, role: :dialog, 'aria-labelledby' => 'explain_command_label', 'aria-hidden' => true}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{type: :button, 'aria-label' => 'Close', data: { dismiss: :modal }}
          %span{'aria-hidden' => true} &times;
        %h4.modal-title#explain_command_label Resource Pack Hash Suggestion
      .modal-body
        %p You should set this to the correct hash value to match the resource-pack.  Doing so helps players log in faster by allowing them to skip the download if the hash hasn't changed.
        %p Suggestion:
        %code resource-pack-hash=#{Preference.latest_resource_pack_hash}
        - if ServerProperties.send(@key).present?
          %p
          %p ... instead of what you have:
          %code resource-pack-hash=#{ServerProperties.send(@key)}

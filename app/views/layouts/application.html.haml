!!!
%html
  %head
    %title CobbleBot
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags

%body

- if admin_signed_in?
  = link_to 'Log out admin', admin_destroy_session_path, method: :delete, data: { confirm: 'Are you sure?' }

- if flash.any?
  .flash
    - flash.each do |type, msg|
      %div{id: type}= msg

= yield

